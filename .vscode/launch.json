{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Vulkan RT",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/VULKAN_RT.exe",
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb"
        }
    ]
}
